import React from "react";
import styles from "./SeatMapSitting.module.css";

const SeatMapSitting = ({ seats }: any) => {
    return (
        <div className={styles.wrapper}>
            <p>Сидячий — схема скоро будет</p>
        </div>
    );
};

export default SeatMapSitting;